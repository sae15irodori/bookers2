<h2>Books</h2>

<% @books.each do |book| %>
<table>
      <thead>
        <tr>
          <th></th>
          <th>Title</th>
          <th>Opinion</th>
          <th></th>
        </tr>
      </thead>

      <tbody>
        <tr>
          <td>ﾕｰｻﾞｰ画像</td>
          <td><%= link_to book.title, book_path(current_user.id) %></td>
          <td><%= book.body %></td>
        </tr>
      </tbody>
    </table>
<% end %>